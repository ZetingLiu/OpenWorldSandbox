---
name: 探索任务数据合成
overview: 使用 GPT-5 基于现有家庭和商超场景自动生成探索型任务。首批生成 4 类任务、每类 2 个环境变体，共 8 个可运行任务，并记录自动重试与人工参与情况。
todos:
  - id: add-task-only
    content: 为 ows gen 增加基于现有场景只生成任务的模式
    status: pending
  - id: draft-pilots
    content: 自动生成并校验 4 个探索任务 pilot
    status: pending
  - id: draft-variants
    content: 为每个 pilot 生成 1 个环境变体
    status: pending
  - id: validate-data
    content: 完成自动门禁、新旧任务回归与在线 smoke test
    status: pending
isProject: true
---

# ARC-AGI-3 风格任务数据合成计划

## 1. 我们要做什么

使用 GPT-5，在现有两个场景上生成需要 Robot **先观察、再行动**的任务：

- [home_01.json](/media/rickliu/OpenWorldSandbox/data/scenarios/home_01.json)
- [market_01.json](/media/rickliu/OpenWorldSandbox/data/scenarios/market_01.json)

首阶段只新增任务 JSON，不修改场景、动作和 Goal DSL。

现有任务继续保留，用于基础能力评测和回归测试；新任务是增量数据，不会替换旧数据。

## 2. 什么叫“需要探索”

任务应满足四点：

1. 指令像正常人说话，而且成功标准没有歧义。
2. 指令不直接告诉 Robot 目标物体的位置、当前状态或完整操作步骤。
3. Robot 能通过观察、打开容器或检查设备获得关键事实。
4. 获得这个事实后，Robot 的目标选择或行动路线会发生变化。

例如：

> 这两个苹果里有一个磕坏了，帮我挑个好的装袋结账。

Robot 知道要挑“没坏的”，但必须查看当前环境才能知道该拿哪个苹果。

需要避免：

- 故意把指令写得含糊，让 Robot 猜作者意图。
- 为了体现探索而要求 Robot 故意执行失败动作。
- 把实体 ID、藏匿位置和操作顺序直接写进指令。
- 用 Goal DSL 限制唯一操作路线；Goal 只判断最终结果。

当前系统还有两个限制：

- `observe_scene` 已经会返回可见实体的状态，不能强行要求每次都调用 `inspect_entity`。
- `search_object` 需要提前知道实体 ID，不能作为真正的通用搜索工具。

## 3. 首批生成哪些任务

首批做 4 类日常问题，每类生成两个版本，共 8 个任务。

“两个版本”指：用户说的话基本相同，但环境状态不同，正确对象或高效路线也不同。这样可以检查 Robot 是真的观察了环境，还是只记住固定 ID 和路线。

### 3.1 找东西

指令示例：

> 洗衣液不在平时的位置，帮我找出来放到洗衣机旁。

- 版本 1：洗衣液藏在一个闭合容器中。
- 版本 2：洗衣液位于另一个合法位置。
- 文件：`home_01_laundry_supply_v1.json`、`home_01_laundry_supply_v2.json`

### 3.2 从候选物中选择

指令示例：

> 这两个苹果里有一个磕坏了，帮我挑个好的装袋结账。

- 版本 1：`apple_01` 状态良好。
- 版本 2：`apple_02` 状态良好。
- 文件：`market_01_pick_good_apple_v1.json`、`market_01_pick_good_apple_v2.json`

### 3.3 判断当前状态

指令示例：

> 我准备睡了，把客厅里还开着的电器关掉。

- 版本 1、2 使用不同的设备运行状态。
- Robot 必须先看当前状态，不能固定操作某一台设备。
- 文件：`home_01_living_room_shutdown_v1.json`、`home_01_living_room_shutdown_v2.json`

### 3.4 发现问题并处理

指令示例：

> 顾客说冷藏柜里没牛奶了，你确认一下并处理好。

- Robot 先确认是否缺货，再寻找可用库存并补货。
- 两个版本改变可用牛奶或库存位置。
- 文件：`market_01_silent_restock_v1.json`、`market_01_silent_restock_v2.json`

如果某个版本为了制造差异而变得不自然，就删除或重新设计，不强行凑满数量。

## 4. GPT-5 如何生成数据

GPT-5 的连接配置已经写入 `.env` 并通过请求测试。

目标流程：

```text
现有场景 + 探索任务 system prompt + 四类明确要求
→ GPT-5 生成候选任务
→ 自动执行 schema、泄漏、去重和探索性检查
→ 自动 compile 回放 walkthrough
→ 失败则带错误原因重新生成，最多 3 次
→ 通过门禁的任务自动进入 data/
→ 全量回归和一次在线 smoke test
→ 输出数据质量与人工参与报告
```

模型原始回复和候选数据先写入 `outputs/synth_staging/`。只有通过全部自动门禁的任务 JSON 才能自动进入 `data/`；原始回复、淘汰候选、临时数据库和审查结果不能进入 `data/`。

当前 `ows gen run` 会同时生成新场景和任务。正式生成前，需要增加一个 **task-only 模式**：读取指定的现有场景，只生成任务。

这套流程沿用 agent-world-model 的数据格式，并吸收 EnvScaler 的核心做法：模型生成候选，程序负责校验可执行性。它不是 EnvScaler 的完整复现。

探索任务使用独立 system prompt；旧任务仅用于 JSON 格式参考，避免继承“显式写出位置和步骤”的指令风格。首批验证完成后，再选一对合格变体作为 few-shot。

## 5. 如何判断任务合格

### 5.1 自动门禁

每个任务必须自动通过：

- JSON 和实体引用合法。
- 指令不包含实体 ID、确切藏匿位置或完整 walkthrough。
- 初始状态中存在可通过合法动作获得的关键事实。
- walkthrough 在关键操作前包含必要的信息获取动作。
- 两个环境变体的正确对象或高效路线确实不同。
- 初始状态尚未完成任务。
- walkthrough 每一步都能成功执行。
- walkthrough 结束后 Goal 成立。
- walkthrough 动作数小于 `max_steps`。
- 原有任务仍能全部编译。

确定性代码负责 schema、实体引用、compile、动作预算和变体差异检查。探索性与语言一致性由一次结构化 GPT-5 审查完成，输出 `pass`、失败原因和五项判断：

- 需要发现什么关键事实？
- 通过什么合法动作发现？
- 发现后哪项决策会变化？
- 是否能靠固定 ID 或固定路线同时通过两个版本？
- instruction 与 Goal DSL 是否一致？

最短有效 walkthrough 作为动作基线。`max_steps` 初始可设为基线的约 1.8 倍，并至少多留 4 步探索空间。

候选失败后不做人工逐条修补，而是把错误原因反馈给 GPT-5 重新生成。达到 3 次上限仍失败则淘汰，并在报告中说明，不为凑满 8 个降低门槛。

### 5.2 本轮人工参与

本轮人工不逐条编辑、不手工走 walkthrough，也不在候选晋级前审批。人工只负责：

1. 事先确定任务类型、场景范围和费用上限。
2. 提供模型配置并启动生成。
3. 阅读最终报告，决定这些实验数据是否继续保留或扩展。

运行过程自动记录候选数、API 调用数、自动重试次数、淘汰原因、人工修改次数、人工决策点和预计人工耗时。后续 PPT 可据此说明“人工参与多少、人工具体做了什么”。

### 5.3 计时与耗时估计

合成程序必须使用单调时钟记录真实耗时，并在 `stats.json` 中保存：

- 整个生成流程的开始时间、结束时间和总耗时。
- 候选生成、GPT-5 结构化审查、自动门禁、compile、重试、数据晋级和全量回归的分阶段耗时。
- 每次 API 请求的耗时、重试次数，以及任务级端到端耗时。
- 在线 smoke test 单独计时，不与纯数据合成耗时混在一起。

dry-run 需要同时输出预计请求数和预计耗时。首轮没有历史数据时使用保守区间；前 2 次真实请求完成后，根据实测 p50 延迟、当前并发数和剩余请求数动态更新 ETA。网络重试时间单独统计，避免把 API 故障误认为数据生成速度。

首批 8 个任务预计需要 16 次基础 GPT-5 调用（8 次生成 + 8 次审查）；候选重新生成时最多增加到 48 次逻辑调用。当前缺少同一接口的实测延迟，先采用以下规划值：

- 并发 1：正常约 15–35 分钟；重试较多时约 30–60 分钟。
- 并发 4：正常约 5–15 分钟；重试较多时约 10–30 分钟。
- compile、自动门禁和全量离线回归通常包含在上述区间内。
- 8 个任务各运行 1 次在线 smoke test 预计额外需要 30–90 分钟。

因此，“生成可编译数据”的初步估计是 **5–35 分钟**；包含在线 smoke test 的完整验证流程初步估计是 **35–120 分钟**。这只是运行前估计，最终报告必须使用本次实测值。

本轮数据合成固定使用 **4 个并发请求**。四个任务族并行执行；每个任务族内部先完成 v1，再将 v1 作为变体上下文生成 v2。在线 smoke test 不沿用这个并发设置，避免多个环境服务端口和运行轨迹互相干扰。

代理接口的实际 GPT-5 单价未知，本轮不猜测货币费用。正式生成固定使用 `max_total_tokens=1000000` 作为硬预算；达到上限立即停止新请求并保留已有结果。费用字段保持 `unknown`，最终报告以真实 token 用量为准。

## 6. 执行顺序

1. 编译全部旧任务，保存基线结果。
2. 给 `ows gen` 增加 task-only 模式、探索任务 prompt 和自动门禁。
3. 以并发 4 执行 dry-run，一次性报告请求数、token、费用上限和预计耗时。
4. 以四个任务族为并发单元，自动生成 4 个 pilot 和 4 个环境变体；失败候选最多自动重试 3 次。
5. 将全部通过门禁的任务自动写入 `data/`，再执行新旧任务全量 compile。
6. 对每个新任务运行 1 次在线 smoke test，验证数据能被正常加载和执行。
7. 输出任务结果、分阶段实测耗时、失败原因和人工参与报告，再由用户决定是否扩展。

## 7. 多关卡机制：先规划，不执行

多关卡用于更接近 ARC-AGI-3 的“逐步发现规则和目标”：

1. 引导关：通过明显反馈展示一个机制。
2. 发现关：去掉操作提示，让 Robot 自己确认规律。
3. 组合关：同时使用前面学到的多个机制。
4. 迁移关：更换实体或布局，检查是否真正学会。

未来可新增 episode manifest，按顺序引用现有 `task_id`；Agent 跨关保留记忆，每关重置环境，并统计分关与总动作数。

该部分目前不修改 schema、Runner 或评测代码。等首批单任务探索数据验证有效后，再单独立项。

## 8. Claude Code 实现契约

为了避免执行者自行猜测，本轮采用以下固定接口和边界：

- 在 `ows.gen.pipeline.Config` 增加 `task_only`、`scenario_paths` 和 `max_task_attempts`；保留现有 `mode=smoke|full` 行为。
- task-only 输入固定为 `data/scenarios/home_01.json,data/scenarios/market_01.json`，不得调用场景生成 API。
- `concurrency` 固定为 4，并继续复用 `GenAPIClient` 的 semaphore；不得自行扩大到 8 或更高。
- `max_total_tokens` 固定为 1000000；不得因为价格字段为空而关闭 token 预算。
- 四个任务族各自作为一个协程：`生成 v1 → 本地校验/compile → 生成 v2 → 本地校验/compile → 成对检查`。
- v2 requirement 必须包含 v1 的 instruction、关键状态和 Goal 摘要，但不能要求模型照抄实体选择。
- 单个候选最多生成 3 次；HTTP 层重试继续使用 `GenAPIClient.max_retries`，两种重试必须分别计数。
- 只有完整通过的 v1/v2 对才能晋级；单独通过一个版本时整对保留在 staging，不进入 `data/`。
- 探索性 GPT-5 审查按单任务执行并输出固定 JSON：`pass`、`key_fact`、`observation_action`、`decision_change`、`fixed_route_risk`、`instruction_goal_consistent`、`reasons`。
- 先在 staging 中完成新任务 compile 和旧任务回归，再复制合格任务；复制后只做最终一致性复核，避免失败数据污染 `data/`。
- 运行目录继续使用 `outputs/synth_staging/<run_id>/`；报告至少包含 `stats.json`、`events.jsonl`、`exploration_reviews.jsonl`、`human_involvement.json` 和最终摘要。
- 修改范围优先限定在 `ows/gen/pipeline.py`、`ows/gen/prompts.py`、`ows/gen/validate.py`、`ows/gen/stats.py` 和对应测试；如需修改场景、动作、Goal DSL 或 Runner，立即停止并说明。

预期命令接口：

```bash
.venv/bin/ows gen run \
  --task_only true \
  --scenario_paths data/scenarios/home_01.json,data/scenarios/market_01.json \
  --max_task_attempts 3 \
  --concurrency 4 \
  --max_total_tokens 1000000 \
  --output_dir outputs/synth_staging
```

实现后先通过 `ows gen run --help` 确认布尔参数的实际写法，再执行 dry-run 和正式生成，不能直接假设解析器语法。

### 8.1 执行 Todo List（供 Claude Code 使用）

执行原则：

- 按顺序完成，不跳过失败项。
- GPT-5 候选先写入 `outputs/synth_staging/`，只有通过全部自动门禁的 Task JSON 可以自动进入 `data/`。
- 不打印、复制或提交 `.env` 中的任何 Key。
- 首批任务只复用 `home_01`、`market_01`；如需修改场景、动作或 Goal DSL，先停止并向用户说明。
- 数据合成并发固定为 4；在线 smoke test 单独顺序执行。
- 正式生成总 token 硬上限为 1000000。
- 不人工修补失败候选；自动重试仍失败时保留错误记录并淘汰。
- 多关卡机制本轮禁止实现。

- [ ] **Step 1：确认当前基线可用**
  - 阅读场景/任务规范、现有生成流程和本计划。
  - 批量编译当前全部任务，保存基线结果。
  - 验收标准：
    - 现有任务全部 `solvable=true`。
    - 没有修改现有 `data/`。
    - 未在日志中输出任何 API Key。

- [ ] **Step 2：增加 task-only 模式和探索 prompt**
  - 允许传入一个或多个现有 scenario，只调用任务生成流程。
  - 调用专用探索任务 system prompt，并为四类任务提供独立 requirement。
  - 候选任务仍经过 schema 校验、去重、compile gate 和 staging。
  - 保持原有“生成 scenario + task”模式可用。
  - 验收标准：
    - task-only 模式不会发起 scenario 生成请求。
    - 配置快照和运行日志中的 `concurrency` 均为 4。
    - prompt 明确要求“意图自然、关键事实需观察、Goal 只约束终态”。
    - prompt 不要求模型创建新实体、动作或 DSL 操作符。
    - 输入不存在或 scenario 非法时给出明确错误。
    - 所有输出都位于 `outputs/synth_staging/<run_id>/`。
    - 原有 full/smoke 模式行为不回归。

- [ ] **Step 3：增加自动门禁和参与记录**
  - 增加指令泄漏、信息动作、变体差异、动作预算和结构化 GPT-5 审查。
  - 记录每个候选的生成、拒绝、重试、compile 和晋级结果。
  - 生成 `human_involvement.json`，记录人工决策点、人工编辑次数和预计耗时。
  - 扩展 `stats.json`，记录总耗时、分阶段耗时、任务级耗时和动态 ETA。
  - 验收标准：
    - 任一门禁失败都会阻止候选进入 `data/`。
    - 错误原因可作为下一次生成的反馈。
    - 默认最多重试 3 次，达到上限后停止该任务族。
    - 使用单调时钟计算 elapsed time，并同时保留可读的开始/结束时间。
    - 在线 smoke test 与纯数据合成分别计时。
    - 报告不包含 API Key 或完整环境变量。

- [ ] **Step 4：自动生成 8 个任务**
  - 先以并发 4 运行 dry-run，报告场景数、目标任务数、最大请求数、token、费用上限和预计耗时。
  - 四个任务族并行；每个任务族内部依次生成 v1 和 v2，并自动执行门禁和有界重试。
  - 验收标准：
    - 找东西、候选选择、状态判断、缺货处理各最多生成 2 个通过候选。
    - 每对任务的正确对象或高效路线不同。
    - 每个晋级候选均符合 Task v0.1 并通过 compile。
    - 任意时刻最多存在 4 个 GPT-5 请求。
    - 只有 v1/v2 均通过时才晋级该任务对。
    - 累计 token 达到 1000000 时停止新请求并正常落盘报告。
    - 不足 8 个时输出实际通过数量，不人工放宽门槛。
    - 前 2 次真实请求后依据实测延迟更新 ETA。

- [ ] **Step 5：自动晋级并执行全量回归**
  - 仅将通过全部门禁的任务 JSON 写入 `data/tasks/home/` 或 `data/tasks/market/`。
  - 批量编译全部旧任务和新任务。
  - 检查 task ID、输出文件和能力标签是否重复或错误。
  - 验收标准：
    - 所有晋级任务均 `solvable=true`。
    - 所有旧任务仍全部通过。
    - 没有候选文件、临时 DB 或原始模型回复进入 `data/`。
    - `git diff --check` 无格式错误。

- [ ] **Step 6：在线 smoke test**
  - 使用被测模型对每个新任务运行 1 次，不以一次成功率作为最终 benchmark 结论。
  - 验收标准：
    - 每个任务都能被 Runner 加载并产生完整轨迹。
    - 报告成功、失败、步数和主要错误。
    - API 或模型故障与任务数据错误分开统计。
    - 分别报告每个任务和全部 smoke test 的耗时。

- [ ] **Step 7：输出数据与人工参与报告**
  - 汇总任务指令、关键事实、观察方式、决策变化、baseline、`max_steps` 和 smoke test 结果。
  - 汇总候选数、API 调用数、自动重试、淘汰原因、总耗时、分阶段耗时、人工决策点、人工编辑次数和预计人工耗时。
  - 验收标准：
    - 用户无需阅读原始模型回复即可判断数据是否值得保留和扩展。
    - 报告明确区分自动步骤与人工步骤。
    - 报告明确区分运行前估计、运行中 ETA 和运行后实测值。
    - 未经用户要求，不 commit、不 push。

- [ ] **Step 8：停止**
  - 本轮不得创建 episode manifest，不得修改多关卡 schema、Runner 或评分器。
  - 验收标准：多关卡内容仍只存在于计划中，没有相关实现改动。
